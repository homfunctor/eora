# todo: configure holy shit it's so convenient for editing text files why have i never tried this before
{
  programs.yazi = {
    enable = true;
    enableFishIntegration = true;
    settings.manager = {
      show_hidden = true;
      sort_dir_first = true;
    };
  };
}
